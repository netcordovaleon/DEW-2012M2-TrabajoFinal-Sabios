<%= form_for(@reply) do |f| %>
  <% if @reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reply.errors.count, "error") %> prohibited this reply from being saved:</h2>

      <ul>
      <% @reply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :query_id %><br />
    <%= f.number_field :query_id, :class => "vacios" %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, :class => "vacios" %>
  </div>
  <div class="field">
    <%= f.label :query %><br />
    <%= f.text_field :query, :class => "vacios" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
